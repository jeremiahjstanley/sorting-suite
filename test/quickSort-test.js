import { expect } from 'chai';
// import { quickSort } from '../lib/quickSort'; 
// import { populateArray, stressTest } from './testing-functions'; 

// let quick = quickSort()
// let populate = populateArray();
// let stress = stressTest();

describe('QuickSort', () => {
  let array;

  it('should sort an unsorted array', () => {
    array = [1, 4, 10, 3, 11, 15, 16]

    // let prototypeMethod = array.sort()

    // expect(quick(array)).to.deep.equal(prototypeMethod());
  })

  // it('should default data property to null', () => {
  //   node = new Node();
  //   expect(node.data).to.equal(null);
  // })
  

}) 

